# /etc/fail2ban/filter.d/virtualbank-litenode.conf
# Detects rate-limit hits (HTTP 429) on the litenode RPC proxy path.

[INCLUDES]
before = common.conf

[Definition]
failregex = ^<HOST> .* "(POST) /rpc.*" 429 .*$
            ^<HOST> .* "(POST) /.*" 429 .*$
ignoreregex =
