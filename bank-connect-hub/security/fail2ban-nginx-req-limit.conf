# /etc/fail2ban/filter.d/nginx-req-limit.conf
# Detects nginx rate-limit rejections logged to error.log.

[Definition]
failregex = ^\d{4}/\d{2}/\d{2} \d{2}:\d{2}:\d{2} \[error\] \d+#\d+: \*\d+ limiting requests, excess: .* by zone .*, client: <HOST>,
ignoreregex =
