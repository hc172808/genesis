# /etc/fail2ban/filter.d/portainer.conf
# Detects repeated 401 responses on the Portainer proxy path.

[INCLUDES]
before = common.conf

[Definition]
failregex = ^<HOST> .* "(GET|POST) /portainer/.*" 401 .*$
ignoreregex =
