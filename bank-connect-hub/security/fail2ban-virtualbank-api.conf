# /etc/fail2ban/filter.d/virtualbank-api.conf
# Detects repeated 401/403 responses on /api/ paths from the nginx access log.

[INCLUDES]
before = common.conf

[Definition]
failregex = ^<HOST> .* "(GET|POST|PUT|DELETE|PATCH) /api/.*" (401|403) .*$
ignoreregex =
